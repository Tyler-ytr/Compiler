int equal(int ax, int ay);
int equal(int zx, int zy) {
  if (zx == zy) {
    return 1;
  } else {
    return 0;
  }
}

int add(int dx, int dy);

int substract(int cx, int cy) {
  return cx - cy;
}

float substractf(float qx, float qy) {
  return qx - qy;
}

int main() {
  int mix = 0, miy = 1;
  float mfx = 1.2, mfy = 4.2;

  int mir;
  float mfr;

  {
    mir = substract(mix, miy);
    if (mir == 0) {
      return 1;
    }

    mfr = substractf(mfx, mfy);
  }

  return 0;
}

int add(int ex, int ey) {
  return ex + ey;
}