struct Vector {
  int x, y;
};

int vec_is_zero(struct Vector Vector) {
  if (Vector.x == 0 && Vector.y == 0) {
    return 1;
  } else {
    return 0;
  }
}

int vec_is_equal(struct Vector v, struct Vector w) {
  if (v.x == w.x && v.y == w.y) {
    return 1;
  } else {
    return 0;
  }
}